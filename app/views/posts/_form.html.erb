<%= form_with(model: @post, url: theme_posts_path) do |f| %>

<%= hidden_field_tag :theme_id, @theme.id %>

<%= f.label :content %>
<%= f.text_area :content, class: 'form-control' %>

<%= f.submit "投稿する" , class: "btn btn-primary" %>
<% end %>